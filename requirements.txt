# Archival dependency export only.
#
# This project uses pyproject.toml as the canonical pip dependency source and
# environment.yml as the Conda/Mamba environment source. The previous contents of
# this file were a machine-specific Conda export containing local build paths,
# so this file is intentionally not used for portable installation.
#
# Use one of:
#   pip install -e .
#   pip install -e ".[dev]"
#   conda env create -f environment.yml
